In Common/CurrentTrainerAccessorExtensions.cs, change GetCurrentTrainerOrErrorAsync to take just the error code (string) instead of a full Error, and build the NotFound error internally with the fixed message "No trainer profile found for the current user." Rename it to GetCurrentTrainerOrNotFoundAsync. Update the call sites in the 5 Clients handlers accordingly. Don't change anything else.