_In Common/CurrentTrainerAccessorExtensions.cs, refactor GetCurrentTrainerOrNotFoundAsync to take a "featurePrefix" string (e.g. "Clients") instead of the full error code, and build the full code internally as "{featurePrefix}.TrainerProfileNotFound", using a private const for the "TrainerProfileNotFound" suffix. Update the 5 call sites in Features/Clients/ to pass "Clients" instead of the full string. Don't change anything else. Show me the diff before applying.